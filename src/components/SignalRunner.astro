<div class="runner-stage" aria-hidden="true">
  <div class="runner-panel">
    <span class="panel-index mono">SUBJECT / 001</span>
    <span class="panel-glyph">界</span>
    <span class="panel-axis axis-x"></span>
    <span class="panel-axis axis-y"></span>
    <span class="panel-target"></span>
  </div>

  <div class="data-ribbon ribbon-a"><span>HUACISHU // SIGNAL ACQUIRED //</span></div>
  <div class="data-ribbon ribbon-b"><span>CREATIVE SYSTEM / ONLINE</span></div>

  <div class="runner-character">
    <div class="scarf-tail scarf-tail-back"></div>
    <div class="coat-back"></div>
    <div class="back-arm"></div>
    <div class="torso">
      <span class="torso-panel"></span>
      <span class="torso-seam seam-a"></span>
      <span class="torso-seam seam-b"></span>
      <span class="torso-code mono">HCS<br />31°N</span>
    </div>
    <div class="neck"></div>
    <div class="head">
      <span class="ear"></span>
      <span class="face-shadow"></span>
      <span class="brow"></span>
      <span class="eye"></span>
      <span class="nose-mark"></span>
      <span class="mouth"></span>
    </div>
    <div class="hair hair-back"></div>
    <div class="hair hair-crown"></div>
    <div class="hair hair-fringe-a"></div>
    <div class="hair hair-fringe-b"></div>
    <div class="hair hair-side"></div>
    <div class="headset"><span></span></div>
    <div class="collar"></div>
    <div class="scarf-tail scarf-tail-front"></div>
    <div class="front-arm">
      <span class="arm-stripe"></span>
      <span class="cuff"></span>
      <span class="glove"><i></i><b></b></span>
    </div>
  </div>

  <div class="impact-lines">
    <span></span><span></span><span></span><span></span><span></span>
  </div>
  <div class="runner-reticle-anchor">
    <div class="runner-reticle"><span></span></div>
  </div>
  <div class="runner-tag mono"><b>LIVE</b><span>PARALLAX<br />SUBJECT 001</span></div>
</div>

<style>
  .runner-stage {
    --cream: #f1efe6;
    --black: #0b0b0a;
    --orange: #ff4f2b;
    --mint: #37c6a2;
    position: absolute;
    z-index: 2;
    top: 9%;
    right: clamp(-22px, 1vw, 18px);
    width: min(64vw, 920px);
    height: 82%;
    min-height: 580px;
    pointer-events: none;
    perspective: 1100px;
    transform-style: preserve-3d;
  }
  .runner-panel {
    position: absolute;
    z-index: 0;
    inset: 9% 9% 8% 11%;
    overflow: hidden;
    border: 2px solid #f1efe6;
    background-color: #d9d8d1;
    background-image:
      linear-gradient(#11111112 1px, transparent 1px),
      linear-gradient(90deg, #11111112 1px, transparent 1px);
    background-size: 28px 28px;
    clip-path: polygon(7% 0, 100% 0, 100% 87%, 91% 100%, 0 100%, 0 8%);
    box-shadow: 20px 22px 0 #ff4f2b;
    transform: translate3d(calc(var(--mx, 0) * -7px), calc(var(--my, 0) * -5px), -70px) rotateY(-2deg);
    transition: transform .25s ease-out;
  }
  .runner-panel::after {
    content: '';
    position: absolute;
    inset: 0;
    opacity: .18;
    background: radial-gradient(circle, #0b0b0a 1px, transparent 1.4px);
    background-size: 7px 7px;
    mask-image: linear-gradient(90deg, #000, transparent 70%);
  }
  .panel-index { position: absolute; z-index: 2; top: 22px; left: 30px; color: #0b0b0a; }
  .panel-glyph { position: absolute; right: -2%; bottom: -13%; color: #0b0b0a; font-size: clamp(230px, 31vw, 470px); line-height: 1; font-weight: 700; opacity: .09; }
  .panel-axis { position: absolute; z-index: 1; background: #0b0b0a42; }
  .axis-x { top: 48%; left: 0; width: 100%; height: 1px; }
  .axis-y { top: 0; left: 53%; width: 1px; height: 100%; }
  .panel-target { position: absolute; top: 43%; left: 48%; width: 10%; aspect-ratio: 1; border: 1px solid #0b0b0a; border-radius: 50%; }
  .panel-target::before, .panel-target::after { content: ''; position: absolute; top: 50%; left: 50%; background: #0b0b0a; transform: translate(-50%, -50%); }
  .panel-target::before { width: 140%; height: 1px; }
  .panel-target::after { width: 1px; height: 140%; }

  .data-ribbon {
    position: absolute;
    z-index: 5;
    overflow: hidden;
    height: 31px;
    border: 1px solid var(--black);
    background: var(--cream);
    color: var(--black);
    font: 500 10px/29px 'DM Mono', monospace;
    white-space: nowrap;
  }
  .data-ribbon span { padding-inline: 12px; }
  .ribbon-a { top: 14%; right: 1%; width: 42%; transform: translate3d(calc(var(--mx, 0) * 10px), calc(var(--my, 0) * 6px), 65px) rotate(7deg); }
  .ribbon-b { right: 0; bottom: 13%; width: 34%; border-color: var(--cream); background: var(--black); color: var(--cream); transform: translate3d(calc(var(--mx, 0) * 14px), calc(var(--my, 0) * 9px), 90px) rotate(-4deg); }

  .runner-character {
    position: absolute;
    z-index: 3;
    top: 2%;
    right: 7%;
    width: 73%;
    height: 99%;
    transform: translate3d(calc(var(--mx, 0) * 8px), calc(var(--my, 0) * 5px), 40px);
    transform-origin: 50% 80%;
    transition: transform .2s ease-out;
  }
  .coat-back {
    position: absolute;
    z-index: 0;
    left: 13%;
    bottom: -8%;
    width: 70%;
    height: 58%;
    border: 5px solid var(--black);
    background: var(--black);
    clip-path: polygon(25% 0, 73% 5%, 100% 36%, 90% 100%, 4% 100%, 0 33%);
    transform: rotate(-2deg);
  }
  .coat-back::after {
    content: '';
    position: absolute;
    inset: 12% 7% 0 55%;
    background: repeating-linear-gradient(135deg, #f1efe625 0 2px, transparent 2px 8px);
  }
  .torso {
    position: absolute;
    z-index: 2;
    left: 25%;
    bottom: -5%;
    width: 58%;
    height: 56%;
    border: 5px solid var(--black);
    background: var(--orange);
    clip-path: polygon(20% 0, 70% 4%, 100% 42%, 88% 100%, 0 100%, 4% 29%);
    transform: rotate(-3deg);
  }
  .torso-panel { position: absolute; inset: 24% 8% 0 53%; border-left: 3px solid var(--black); background: #f1efe614; transform: skewX(-8deg); }
  .torso-seam { position: absolute; height: 3px; background: var(--black); transform-origin: left; }
  .seam-a { top: 36%; left: 7%; width: 44%; transform: rotate(11deg); }
  .seam-b { top: 53%; left: 46%; width: 41%; transform: rotate(-57deg); }
  .torso-code { position: absolute; right: 10%; bottom: 13%; padding: 8px; border: 2px solid var(--black); color: var(--black); font-size: 9px; }
  .neck { position: absolute; z-index: 3; top: 34%; left: 45%; width: 22%; height: 22%; border: 5px solid var(--black); background: var(--cream); transform: rotate(8deg) skewX(-7deg); }
  .head {
    position: absolute;
    z-index: 5;
    top: 8%;
    left: 26%;
    width: 42%;
    height: 40%;
    border: 5px solid var(--black);
    background: var(--cream);
    clip-path: polygon(18% 7%, 70% 0, 98% 28%, 91% 57%, 73% 60%, 64% 88%, 39% 100%, 14% 83%, 7% 48%, 0 26%);
    transform: rotate(-5deg);
  }
  .head::after { content: ''; position: absolute; right: 1%; bottom: 13%; width: 27%; height: 3px; background: var(--black); transform: rotate(-18deg); }
  .ear { position: absolute; z-index: 2; right: 2%; top: 46%; width: 20%; aspect-ratio: 1; border: 4px solid var(--black); border-radius: 50%; background: var(--orange); }
  .face-shadow { position: absolute; left: 0; bottom: 0; width: 45%; height: 51%; background: var(--orange); clip-path: polygon(0 5%, 100% 31%, 68% 100%, 0 100%); opacity: .9; }
  .brow { position: absolute; z-index: 3; top: 37%; left: 43%; width: 28%; height: 4px; background: var(--black); transform: rotate(-7deg); }
  .eye { position: absolute; z-index: 3; top: 43%; left: 52%; width: 12%; height: 7px; border-bottom: 4px solid var(--black); transform: rotate(-7deg); }
  .eye::after { content: ''; position: absolute; right: 1px; top: 3px; width: 5px; height: 7px; background: var(--mint); }
  .nose-mark { position: absolute; z-index: 3; top: 52%; left: 70%; width: 12%; height: 16%; border-left: 3px solid var(--black); border-bottom: 3px solid var(--black); transform: skewX(-18deg); }
  .mouth { position: absolute; z-index: 3; top: 69%; left: 65%; width: 18%; height: 3px; background: var(--black); transform: rotate(-8deg); }
  .hair { position: absolute; z-index: 7; background: var(--black); }
  .hair-back { top: 6%; left: 19%; width: 28%; height: 43%; clip-path: polygon(22% 0, 100% 11%, 81% 100%, 42% 80%, 0 43%); transform: rotate(-7deg); }
  .hair-crown { top: 1%; left: 27%; width: 46%; height: 24%; clip-path: polygon(0 54%, 13% 18%, 28% 37%, 47% 0, 56% 32%, 82% 10%, 78% 42%, 100% 39%, 85% 100%, 20% 93%); transform: rotate(-4deg); }
  .hair-fringe-a { top: 16%; left: 43%; width: 23%; height: 25%; clip-path: polygon(0 0, 100% 8%, 39% 100%); transform: rotate(-8deg); }
  .hair-fringe-b { top: 16%; left: 56%; width: 21%; height: 20%; clip-path: polygon(0 0, 100% 21%, 44% 100%); transform: rotate(4deg); }
  .hair-side { top: 20%; left: 30%; width: 16%; height: 29%; clip-path: polygon(0 0, 100% 12%, 62% 100%, 8% 77%); transform: rotate(-3deg); }
  .headset { position: absolute; z-index: 8; top: 21%; left: 27%; width: 13%; height: 17%; border: 4px solid var(--mint); background: var(--black); transform: rotate(-10deg); }
  .headset span { position: absolute; top: 35%; left: 25%; width: 50%; height: 3px; background: var(--mint); }
  .collar { position: absolute; z-index: 6; top: 40%; left: 35%; width: 43%; height: 16%; border: 5px solid var(--black); background: var(--mint); clip-path: polygon(7% 0, 100% 31%, 78% 100%, 0 75%); transform: rotate(8deg); }
  .back-arm { position: absolute; z-index: 1; left: 2%; bottom: 13%; width: 34%; height: 30%; border: 5px solid var(--black); background: var(--cream); clip-path: polygon(13% 0, 100% 45%, 91% 100%, 0 54%); transform: rotate(11deg); }
  .front-arm {
    position: absolute;
    z-index: 9;
    right: -33%;
    bottom: 20%;
    width: 84%;
    height: 25%;
    border: 5px solid var(--black);
    background: var(--orange);
    clip-path: polygon(0 11%, 75% 28%, 100% 49%, 80% 73%, 14% 96%);
    transform: rotate(-12deg);
    transform-origin: 8% 50%;
    animation: runner-arm-idle 3.8s ease-in-out infinite alternate;
  }
  .arm-stripe { position: absolute; top: 18%; left: 17%; width: 9%; height: 74%; background: var(--cream); border-inline: 3px solid var(--black); transform: skewX(-11deg); }
  .cuff { position: absolute; z-index: 2; right: 14%; top: 11%; width: 16%; height: 80%; border: 4px solid var(--black); background: var(--mint); transform: skewX(-11deg); }
  .glove { position: absolute; z-index: 3; right: -1%; top: 7%; width: 19%; height: 87%; border: 4px solid var(--black); background: var(--cream); clip-path: polygon(0 0, 72% 16%, 100% 44%, 75% 81%, 14% 100%); }
  .glove i, .glove b { position: absolute; right: 9%; width: 56%; height: 3px; background: var(--black); transform: rotate(14deg); }
  .glove i { top: 38%; } .glove b { top: 57%; }
  .scarf-tail { position: absolute; border: 5px solid var(--black); }
  .scarf-tail-back { z-index: 1; top: 39%; left: 3%; width: 46%; height: 13%; background: var(--mint); clip-path: polygon(0 12%, 100% 0, 91% 76%, 13% 100%); transform: rotate(11deg); }
  .scarf-tail-front { z-index: 8; top: 43%; left: 7%; width: 42%; height: 11%; background: var(--cream); clip-path: polygon(0 23%, 100% 0, 86% 100%, 13% 77%); transform: rotate(-7deg); }

  .impact-lines { position: absolute; z-index: 7; right: -6%; bottom: 28%; width: 39%; height: 38%; transform: translate3d(calc(var(--mx, 0) * 16px), calc(var(--my, 0) * 10px), 100px); transition: transform .18s ease-out; }
  .impact-lines span { position: absolute; right: 0; width: var(--w); height: 3px; background: var(--cream); transform: rotate(var(--r)); transform-origin: right; animation: impact-flicker 2.8s ease-in-out infinite alternate; animation-delay: calc(var(--line, 0) * -180ms); }
  .impact-lines span:nth-child(1) { --w: 80%; --r: -24deg; top: 8%; }
  .impact-lines span:nth-child(2) { --w: 45%; --r: -11deg; --line: 1; top: 27%; }
  .impact-lines span:nth-child(3) { --w: 75%; --r: 5deg; --line: 2; top: 49%; background: var(--orange); }
  .impact-lines span:nth-child(4) { --w: 52%; --r: 18deg; --line: 3; top: 68%; }
  .impact-lines span:nth-child(5) { --w: 82%; --r: 29deg; --line: 4; top: 88%; }
  .runner-reticle-anchor { position: absolute; z-index: 8; top: 17%; left: 5%; width: 88px; aspect-ratio: 1; pointer-events: none; will-change: transform; }
  .runner-reticle { position: absolute; inset: 0; border: 1px solid var(--cream); border-radius: 50%; pointer-events: none; }
  .runner-reticle::before, .runner-reticle::after { content: ''; position: absolute; top: 50%; left: 50%; background: var(--orange); transform: translate(-50%, -50%); }
  .runner-reticle::before { width: 125%; height: 2px; } .runner-reticle::after { width: 2px; height: 125%; }
  .runner-reticle span { position: absolute; inset: 20%; border: 1px solid var(--cream); border-radius: 50%; }
  .runner-reticle { animation: reticle-turn 18s linear infinite; }
  .runner-tag { position: absolute; z-index: 9; left: 3%; bottom: 10%; display: flex; align-items: center; gap: 9px; color: var(--cream); transform: translateZ(80px); }
  .runner-tag b { padding: 6px 7px; background: var(--orange); color: var(--black); font-weight: 500; }
  .runner-tag span { font-size: 9px; }
  @keyframes runner-arm-idle { to { rotate: 1.6deg; translate: 3px -2px; } }
  @keyframes impact-flicker { 55% { scale: .78 1; opacity: .5; } }
  @keyframes reticle-turn { to { rotate: 360deg; } }

  @media (max-width: 1000px) {
    .runner-stage { right: -7vw; width: 72vw; }
    .runner-character { right: 1%; }
    .runner-reticle-anchor, .runner-tag { display: none; }
  }
  @media (max-width: 760px) {
    .runner-stage { top: 18%; right: -20%; width: 110%; height: 64%; min-height: 420px; perspective: none; }
    .runner-panel { inset: 8% 9% 4% 14%; box-shadow: 11px 13px 0 var(--orange); transform: none; }
    .runner-panel::after { opacity: .1; }
    .panel-index { top: 14px; left: 18px; font-size: 8px; }
    .panel-glyph { font-size: 270px; }
    .runner-character { top: 0; right: 3%; width: 75%; height: 100%; transform: none; }
    .ribbon-a { top: 12%; right: 7%; width: 45%; }
    .ribbon-b { right: 5%; bottom: 5%; width: 40%; }
    .impact-lines { right: 2%; bottom: 25%; width: 35%; transform: none; }
    .front-arm { right: -27%; }
  }
  @media (max-width: 420px) {
    .runner-stage { top: 19%; right: -27%; width: 122%; height: 61%; }
    .runner-panel { inset: 10% 9% 2% 16%; }
    .runner-character { right: 4%; width: 78%; }
    .ribbon-a { right: 12%; }
    .ribbon-b { display: none; }
  }
  @media (prefers-reduced-motion: reduce) {
    .runner-panel, .runner-character, .impact-lines, .front-arm, .runner-reticle-anchor, .runner-reticle, .impact-lines span { transform: none; transition: none; animation: none; }
  }
</style>
