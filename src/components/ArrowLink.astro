---
import { ArrowUpRight } from 'lucide-astro';
const { href, label, external = false } = Astro.props;
---
<a class="rule-link" href={href} target={external ? '_blank' : undefined} rel={external ? 'noreferrer' : undefined}>{label}<ArrowUpRight size={15} /></a>
